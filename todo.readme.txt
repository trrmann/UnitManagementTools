X refactor the project to instantiate the storage object and the cachestore object inside that object as early as possible for use by any component that needs it for site operation, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
X on the testing tab, the view button in the cache button group is not displaying the cache class data entries from the storage class, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
X refactor the testing tab to use a modal display element in order to view cache data after clicking the view button in the cache button group on the testing tab instead of the current popup, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
X refactor the testing tab export button in the cache button group to export the cache class data entries from the storage class as a json, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab import button in the cache button group to import and overwrite the cache class data entries from the storage class as a json, use the storage class to interface with these writes, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

on the testing tab, the view raw button in the users button group is not displaying the users class user entries data, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab to use a modal display element in order to view raw users data after clicking the view raw button in the users button group on the testing tab instead of the current popup, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab export raw button in the users button group to export the users class user entries data as a json, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab import raw button in the users button group to import the users class user entries data as a json and save the update to the google drive, local storage with a default expire time, session storage with a default storage time and the cache storage with a default expire time, use the storage class to interface with these writes, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
on the testing tab, the view detailed button in the users button group is not displaying the users class user details data, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab to use a modal display element in order to view detailed users data after clicking the view detailed button in the users button group on the testing tab instead of the current popup, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab export detailed button in the users button group to export the users class user details data as a json, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab import detailed button in the users button group to mock import the users class user details data as a json using a mock conversion routine from detailed to raw data and mock save the update to the google drive, local storage with a default expire time, session storage with a default storage time and the cache storage with a default expire time, use the storage class to interface with these writes, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the users tab export users button to call the exact same function for the testing tab export detailed button in the users button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the users tab import users button to call the exact same function for the testing tab import detailed button in the users button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

on the testing tab, the view raw button in the members button group is not displaying the members class member entries data, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab to use a modal display element in order to view raw members data after clicking the view raw button in the members button group on the testing tab instead of the current popup, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab export raw button in the members button group to export the members class member entries data as a json, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab import raw button in the members button group to import the members class member entries data as a json and save the update to the google drive, local storage with a default expire time, session storage with a default storage time and the cache storage with a default expire time, use the storage class to interface with these writes, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
on the testing tab, the view detailed button in the members button group is not displaying the members class member details data, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab to use a modal display element in order to view detailed members data after clicking the view detailed button in the members button group on the testing tab instead of the current popup, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab export detailed button in the members button group to export the members class member details data as a json, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab import detailed button in the members button group to mock import the members class member details data as a json using a mock conversion routine from detailed to raw and mock save the update to the google drive, local storage with a default expire time, session storage with a default storage time and the cache storage with a default expire time, use the storage class to interface with these writes, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab to add a sync button to the members button group to mock the syncing of member information from the church web sites, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the users tab export members button to call the exact same function for the testing tab export detailed button in the members button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the users tab import members button to call the exact same function for the testing tab import detailed button in the members button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the users tab sync members button to call the exact same function for the testing tab sync button in the members button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the members tab add an export members button that calls the exact same function for the testing tab export detailed button in the members button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the members tab add an import members button that calls the exact same function for the testing tab import detailed button in the members button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the members tab sync members button to call the exact same function for the testing tab sync button in the members button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

on the testing tab, the view raw button in the roles button group is not displaying the roles class role entries data, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab to use a modal display element in order to view raw roles data after clicking the view raw button in the roles button group on the testing tab instead of the current popup, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab export raw button in the roles button group to export the roles class role entries data as a json, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab import raw button in the roles button group to import the roles class role entries data as a json and save the update to the google drive, local storage with a default expire time, session storage with a default storage time and the cache storage with a default expire time, use the storage class to interface with these writes, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
on the testing tab, the view detailed button in the roles button group is not displaying the roles class role details data, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab to use a modal display element in order to view detailed roles data after clicking the view detailed button in the roles button group on the testing tab instead of the current popup, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab export detailed button in the roles button group to export the roles class role details data as a json, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab import detailed button in the roles button group to mock import the roles class role details data as a json using a mock conversion routine from detailed to raw and mock save the update to the google drive, local storage with a default expire time, session storage with a default storage time and the cache storage with a default expire time, use the storage class to interface with these writes, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the roles tab to update the export button that calls the exact same function for the testing tab export detailed button in the roles button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the roles tab to update the import button that calls the exact same function for the testing tab import detailed button in the roles button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

on the testing tab, the view button in the callings button group is not displaying the callings class calling entries data, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab to use a modal display element in order to view raw callings data after clicking the view button in the callings button group on the testing tab instead of the current popup, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab export button in the callings button group to export the callings class calling entries data as a json, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab import button in the callings button group to import the callings class calling entries data as a json and save the update to the google drive, local storage with a default expire time, session storage with a default storage time and the cache storage with a default expire time, use the storage class to interface with these writes, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab to add a sync button to the callings button group to mock the syncing of calling information from the church web sites, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the callings tab to update the export button that calls the exact same function for the testing tab export button in the callings button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the callings tab to update the import button that calls the exact same function for the testing tab import button in the callings button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the callings tab to update the sync button to call the exact same function for the testing tab sync button in the callings button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

on the testing tab, the view button in the organization button group is not displaying the organization class org entries data, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab to use a modal display element in order to view raw organization data after clicking the view button in the organization button group on the testing tab instead of the current popup, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab export button in the organization button group to export the organization class org entries data as a json, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab import button in the organization button group to import the organization class org entries data as a json and save the update to the google drive, local storage with a default expire time, session storage with a default storage time and the cache storage with a default expire time, use the storage class to interface with these writes, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the organization tab to update the export button that calls the exact same function for the testing tab export button in the organization button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the organization tab to update the import button that calls the exact same function for the testing tab import button in the organization button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

on the testing tab, the view button in the configuration button group is not displaying the configuration class config entries data, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab to use a modal display element in order to view raw configuration data after clicking the view button in the configuration button group on the testing tab instead of the current popup, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab export button in the configuration button group to export the configuration class config entries data as a json, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab import button in the configuration button group to import the configuration class config entries data as a json and save the update to the google drive, local storage with a default expire time, session storage with a default storage time and the cache storage with a default expire time, use the storage class to interface with these writes, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the configuration tab to update the export button that calls the exact same function for the testing tab export button in the configuration button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the configuration tab to update the import button that calls the exact same function for the testing tab import button in the configuration button group, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

on the testing tab, the view button in the cloud storage button group is not displaying the list of files stored on googleDrive, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab to use a modal display element in order to view the list of files stored on googleDrive after clicking the view button in the cloud storage button group on the testing tab insted of the current popup, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab export button in the cloud storage button group to be named download and to provide a selector or available files to download in a modal element one selected then download the selected file from googleDrive providing an option to select the save location and filename of the downloaded file locally, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab import button in the cloud storage button group to be named upload and provide an explorer window to select a file to upload, once selected provide a modal window to name the file on googleDrive once commited then upload the file, giving an option to overwrite or cancel on files already on the googleDrive, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

on the testing tab, the view button in the github data button group is not displaying the list of files stored on github in the data folder, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab to use a modal display element in order to view the list of files stored on github in the data folder after clicking the view button in the github data button group on the testing tab insted of the current popup, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the testing tab export button in the github data button group to be named download and to provide a selector or available files to download in a modal element one selected then download the selected file from the data folder on github providing an option to select the save location and filename of the downloaded file locally, add and/or adjust any unit tests to ensure this functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

refactor the functions in callings.ui.js to be moved (including removed from the orginal file) into the callings class in the callings module without loosing any functionality.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the remaining code to be a callable static function and move this into the callings class in the callings module without loosing any functionality.  refactor the calling mechanisms to ensure this new functionality is implemented on page load.  remove the requirement of the callings.ui.js at all in this step.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

refactor the functions in configuration.ui.js to be moved (including removed from the orginal file) into the configuration class in the configuration module without loosing any functionality.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the remaining code to be a callable static function and move this into the configuration class in the configuration module without loosing any functionality.  refactor the calling mechanisms to ensure this new functionality is implemented on page load.  remove the requirement of the configuration.ui.js at all in this step.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

refactor the functions in eventscheduletemplate.ui.js to be moved (including removed from the orginal file) into the eventscheduletemplate class in the eventscheduletemplate module without loosing any functionality.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the remaining code to be a callable static function and move this into the eventscheduletemplate class in the eventscheduletemplate module without loosing any functionality.  refactor the calling mechanisms to ensure this new functionality is implemented on page load.  remove the requirement of the eventscheduletemplate.ui.js at all in this step.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

refactor the functions in members.ui.js to be moved (including removed from the orginal file) into the members class in the members module without loosing any functionality.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the remaining code to be a callable static function and move this into the members class in the members module without loosing any functionality.  refactor the calling mechanisms to ensure this new functionality is implemented on page load.  remove the requirement of the members.ui.js at all in this step.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

refactor the functions in organization.ui.js to be moved (including removed from the orginal file) into the organization class in the organization module without loosing any functionality.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the remaining code to be a callable static function and move this into the organization class in the organization module without loosing any functionality.  refactor the calling mechanisms to ensure this new functionality is implemented on page load.  remove the requirement of the organization.ui.js at all in this step.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

refactor the functions in roles.ui.js to be moved (including removed from the orginal file) into the roles class in the roles module without loosing any functionality.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the remaining code to be a callable static function and move this into the roles class in the roles module without loosing any functionality.  refactor the calling mechanisms to ensure this new functionality is implemented on page load.  remove the requirement of the roles.ui.js at all in this step.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

refactor the functions in testing.ui.js to be moved (including removed from the orginal file) into the testing class in the testing module without loosing any functionality.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the remaining code to be a callable static function and move this into the testing class in the testing module without loosing any functionality.  refactor the calling mechanisms to ensure this new functionality is implemented on page load.  remove the requirement of the testing.ui.js at all in this step.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

refactor the functions in users.ui.js to be moved (including removed from the orginal file) into the users class in the users module without loosing any functionality.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the remaining code to be a callable static function and move this into the users class in the users module without loosing any functionality.  refactor the calling mechanisms to ensure this new functionality is implemented on page load.  remove the requirement of the users.ui.js at all in this step.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

refactor the functions in workflows.ui.js to be moved (including removed from the orginal file) into the workflows class in the workflows module without loosing any functionality.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the remaining code to be a callable static function and move this into the workflows class in the workflows module without loosing any functionality.  refactor the calling mechanisms to ensure this new functionality is implemented on page load.  remove the requirement of the workflows.ui.js at all in this step.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

refactor the functions in script.js to be moved (including removed from the orginal file) into the site class in the site module without loosing any functionality.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the remaining code to be a callable static function and move this into the site class in the site module without loosing any functionality.  refactor the calling mechanisms to ensure this new functionality is implemented on page load by calling this new function from the script.js.  remove the requirement of the callings.ui.js at all in this step.  refactor all test cases to match the new model without loosing any functionality.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

refactor the main element of the index.html to be built dynamically in JavaScript from the site class in the site module instead of hard coded in html. implement the creation into the html by calling a static function inside the site class that builds the body elements.  call the body element building function from the script.js.
refactor the creation of the dashboard section with id="dashboard" to be built dynamically in JavaScript in the eventscheduletemplate class in the eventscheduletemplate module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the creation of the assignments section with id="assignments" to be built dynamically in JavaScript in the eventscheduletemplate class in the eventscheduletemplate module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the creation of the scheduling section with id="scheduling" to be built dynamically in JavaScript in the eventscheduletemplate class in the eventscheduletemplate module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the creation of the forms section with id="forms" to be built dynamically in JavaScript in the eventscheduletemplate class in the eventscheduletemplate module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the creation of the reports section with id="reports" to be built dynamically in JavaScript in the eventscheduletemplate class in the eventscheduletemplate module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the creation of the testing section with id="testing" to be built dynamically in JavaScript in the eventscheduletemplate class in the eventscheduletemplate module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the creation of the eventscheduletemplate section with id="eventscheduletemplate" to be built dynamically in JavaScript in the eventscheduletemplate class in the eventscheduletemplate module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the creation of the workflows section with id="workflows" to be built dynamically in JavaScript in the workflows class in the workflows module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the creation of the users section with id="users" to be built dynamically in JavaScript in the users class in the users module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the creation of the roles section with id="roles" to be built dynamically in JavaScript in the roles class in the roles module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the creation of the callings section with id="callings" to be built dynamically in JavaScript in the callings class in the callings module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the creation of the organization section with id="organization" to be built dynamically in JavaScript in the organization class in the organization module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the creation of the configuration section with id="configuration" to be built dynamically in JavaScript in the configuration class in the configuration module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the creation of the members section with id="members" to be built dynamically in JavaScript in the members class in the members module.  implement the addition of this section to the main content in the site class in the site module main element building function.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
add a header class in a header module.  refactor the header section of the index.html to be built dynamically in JavaScript from the header class instead of hard coded in html. implement the creation into the html by calling a static function inside the header class from the site class.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
add a footer class in a footer module.  create a footer section to be built dynamically in JavaScript from the footer class instead of hard coded in html.  implement the display of the new footer into the index.html from the footer class.   implement the creation into the html by calling a static function inside the footer class from the site class.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
add a navigation class in a navigation module.  refactor the navigation section of the index.html to be built dynamically in JavaScript from the navigation class instead of hard coded in html.   implement the creation into the html by calling a static function inside the navigation class from the site class.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
add a modal class in a modal module.  refactor the modal section of the index.html to be built dynamically in JavaScript from the modal class instead of hard coded in html.   implement the creation into the html by calling a static function inside the modal class from the site class.  create all required unit tests to validate these new requirements.  perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.

refactor the objectUtils module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the githubdata module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the googledrive module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the localstorage module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the sessionstorage module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the cachestore module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the storage module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the crypto module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the configuration module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the org module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the callings module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the roles module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the members module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the users module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the auth module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the workflows module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the eventscheduletemplate module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the header module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the footer module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the navigation module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the modal module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
refactor the site module for optimizations of fast paths, performance, memory, concurrency, API ergonomics, code efficency, complete unit testing, fully documented processes, and project standarization, perform only 1 next indiviudal update at a time with any implementations that are required with it to maintain functionality for now for review that no functionality is lost verified with valid unit tests, add new unit tests for any new functionality.  when an update is verfied as passing all valid unit tests perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.


update the access rights data to allow only the system admin, developer and tester to view the configuration tab.  add unit tests for all functionality, perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
update the access rights data to allow only the system admin, developer and tester to view the organization tab.  add unit tests for all functionality, perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
update the access rights data to allow only the system admin, developer and tester to view the callings tab.  add unit tests for all functionality, perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
update the access rights data to allow only the system admin, developer and tester to view the roles tab.  add unit tests for all functionality, perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
update the access rights data to allow only the system admin, developer and tester to view the users tab.  add unit tests for all functionality, perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
update the access rights data to allow only the system admin, developer and tester to view the testing tab.  add unit tests for all functionalitym perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
update the access rights data to allow only the system admin to view the workflows tab.  add unit tests for all functionalitym perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.
update the access rights data to allow only the system admin to view the 'event schedule template' tab.  add unit tests for all functionality, perform a full regression test of all previously passing test cases one file at a time and fix any errors created by this update.



add unit tests to verify the buttons and functionality exists in the expected locations, then perform a full regression test of all previously passing test cases one file at a time one file at a time and fix any errors created by this update.



add cloud storage config - to view/edit the googledrive.json data on the config tab

refactor the user data to add a google login field.  add unit tests for all functionality
refactor to validate the logged in user is oauth authenticated with thier google login or warn that full functionality is not available.  add unit tests for all functionality

refactor the user data to add a church login field.  add unit tests for all functionality
refactor the user data to add a church password field.  add unit tests for all functionality
refactor the user data to add a church id state field.  includes the following states:  requested, new, updated, verified, failed.  add unit tests for all functionality
add an attempt to authenticate the church userid and password and save the the state to verified or failed based on the results of the attempt if the state is new or updated.  warn that full functionality is not available if the state is failed for the current logged in user.  add unit tests for all functionality
refactor the user details to add a church login field.  add unit tests for all functionality
refactor the user details to add a church password field.  add unit tests for all functionality
refactor the user details to add a church id state field.  add unit tests for all functionality
refactor to add the church login and church id state to the users tab data for each user.  add unit tests for all functionality
add an update church credentials button to each user on the users tab.  add unit tests for all functionality



//TODO #24 add tasks module - integrate into assignments tab
//TODO #28 add schedule module integrate into the schedule tab
//TODO #29 add events module integrate into the schedule module

//TODO #30 first workflow is the calling pipeline
//TODO #31 second workflow is the sacrament talk pipeline
//TODO #32 third workflow is the sacrament prayers pipline
//TODO #33 forth workflow is the youth temple recommend pipline
//TODO #34 fifth workflow is the missing members pipline

//TODO #35 first repeat event schedule is ward council meeting
//TODO #36 second repeat event schedule is bishopric meeting
//TODO #37 third repeat event schedule is sacrament attendance count
//TODO #38 forth repeat event schedule is donation processing
//TODO #39 fifth repeat event schedule is expense review
//TODO #40 sixth repeat event schedule is tithing declaration
//TODO #41 seventh repeat event schedule is leadership youth training
//TODO #42 eighth repeat event schedule is leadership church funds training
